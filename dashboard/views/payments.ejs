<% layout('layout', { title: 'Payments' }) %>
<h1>💰 Payments</h1>
<table class="table table-bordered">
  <thead><tr><th>Date</th><th>Phone</th><th>Amount</th><th>Status</th></tr></thead>
  <tbody>
    <% payments.forEach(p => { %>
      <tr>
        <td><%= p.date %></td>
        <td><%= p.phone %></td>
        <td>KES <%= p.amount %></td>
        <td><%= p.status %></td>
      </tr>
    <% }) %>
  </tbody>
</table>

